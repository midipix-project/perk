tree.tag:
		mkdir -p src
		mkdir -p src/arbits
		mkdir -p src/arbits/output
		mkdir -p src/cmds
		mkdir -p src/driver
		mkdir -p src/hexdump
		mkdir -p src/info
		mkdir -p src/internal
		mkdir -p src/logic
		mkdir -p src/output
		mkdir -p src/reader
		mkdir -p src/skin
		touch tree.tag
